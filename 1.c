#include <stdio.h>
#include <string.h>
int main()
{   
char ch;
char s[100];
char sen[100];
    scanf("%c",&ch);
     gets(s);
     gets(sen);

    printf("%c",ch);
      printf("%s",s);
       printf("%s",sen);
//
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */

}
